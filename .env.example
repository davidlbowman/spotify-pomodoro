# Spotify App Credentials
# Create an app at https://developer.spotify.com/dashboard
PUBLIC_SPOTIFY_CLIENT_ID=your_client_id_here
PUBLIC_SPOTIFY_REDIRECT_URI=http://localhost:2500/callback

# Optional: customize port (default: 2500)
# PORT=2500

# Authentication (optional - for VPS deployments)
# Set AUTH_ENABLED=true to require login
# Username is always "admin"
AUTH_ENABLED=false
AUTH_PASSWORD=changeme
AUTH_SECRET=generate-a-random-32-character-string-here
